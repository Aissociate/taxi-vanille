node_modules/
.expo/
.expo-shared/
dist/
web-build/
# .env est committé (URL prod publique, pas de secret).
# .env.local OU .env.development.local pour les overrides dev (localhost) — gitignored.
.env.local
.env.development.local
.env.production.local
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
google-services.json
GoogleService-Info.plist
